--- a/evutil_rand.c
+++ b/evutil_rand.c
@@ -173,8 +173,9 @@ evutil_secure_rng_get_bytes(void *buf, size_t n)
 
 void
 evutil_secure_rng_add_bytes(const char *buf, size_t n)
 {
-	arc4random_addrandom((unsigned char*)buf,
-	    n>(size_t)INT_MAX ? INT_MAX : (int)n);
+	/* arc4random_addrandom is no longer available in modern glibc. */
+	/* This function is effectively a no-op now. */
+	(void)buf; (void)n;
 }
 
 
